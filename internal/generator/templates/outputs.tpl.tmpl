MCP Server deployed successfully

The MCP server "{{.ServerName}}" has been deployed to Nomad.

To view the job status:
  nomad job status {{.PackName}}

To view allocation logs:
  nomad alloc logs <alloc-id>